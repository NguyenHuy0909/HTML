Function NextEmptyCol(ws As Worksheet, startCol As Long, dataRow As Long) As Long
    Dim col As Long
    col = startCol
    Do While ws.Cells(dataRow, col).Value <> ""
        col = col + 1
    Loop
    NextEmptyCol = col
End Function
