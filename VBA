     ║ For modelIdx = LBound(xData, 1) To UBound(xData, 1)       ║
     ║   For groupIdx = LBound(xData, 2) To UBound(xData, 2)     ║
     ║     xv = CDbl(xData(modelIdx, groupIdx))                  ║
     ║     frac = (xv - xMin) / (xMax - xMin)                    ║
     ║     wsX = paL + frac * paW + xOffsetRgt                   ║
     ║     DrawVerticalLine(ws, wsX, ...)                        ║
     ║   Next groupIdx                                           ║
     ║ Next modelIdx       
