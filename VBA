Dim r As Long
Dim c As Long

For r = 1 To Application.WorksheetFunction.Min(5, UBound(dataArr, 1))

    For c = LBound(dataArr, 2) To UBound(dataArr, 2)
        Debug.Print dataArr(r, c);
    Next c
    
    Debug.Print

Next r
