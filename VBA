Function GetGIDHeaderArray(ts As Object, headerName As String) As Variant

    Dim line As String
    Dim buffer As String
    Dim parts As Variant
    Dim arr() As Variant
    Dim i As Long, n As Long

    buffer = ""

    ts.Seek 0   'quay lại đầu file

    Do Until ts.AtEndOfStream

        line = ts.ReadLine

        If InStr(1, line, headerName, vbBinaryCompare) > 0 Then
        
            buffer = line
            
            If Not ts.AtEndOfStream Then
                line = ts.ReadLine
                buffer = buffer & line
            End If
            
            buffer = Replace(buffer, "&", "")
            
            Exit Do
            
        End If

    Loop

    parts = Split(buffer, "'")

    n = UBound(parts) \ 2
    ReDim arr(1 To n)

    For i = 1 To n
        arr(i) = Trim(parts(2 * i))
    Next i

    GetGIDHeaderArray = arr

End Function
